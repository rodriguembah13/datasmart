{% extends 'base.html.twig' %}

{% block title %}Edit Documentaire{% endblock %}

{% block body %}
    <h1>Edit Documentaire</h1>

    {{ include('documentaire/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('documentaire_index') }}">back to list</a>

    {{ include('documentaire/_delete_form.html.twig') }}
{% endblock %}
