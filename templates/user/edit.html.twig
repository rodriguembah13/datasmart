{% extends 'base.html.twig' %}

{% block title %}Edit User{% endblock %}
{% block page_title %} User {% endblock %}
{% block page_subtitle %} edit {% endblock %}
{% block page_content %}

    {{ include('user/_form.html.twig', {'button_label': 'Update'}) }}
    <a class="btn btn-warning" href="{{ path('user_index') }}"><i class="fa fa-arrow-left"></i>back to list</a>
    {#<div class="row">


        <div class="col-md-9"> <a class="btn btn-warning" href="{{ path('user_index') }}">back to list</a>

            <a class="btn btn-primary" href="{{ path('user_edit', {'id': user.id}) }}">edit</a></div>
        <div class="col-md-3"> {{ include('user/_delete_form.html.twig') }}</div>
    </div>#}
{% endblock %}
