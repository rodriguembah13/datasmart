{% form_theme form '@AdminLTE/layout/form-theme-horizontal.html.twig' %}
{{ form_start(form) }}{{  form_row(form.libelle) }}
<button class="btn btn-primary pull-left">{{ button_label|default('Save') }}</button>
{#<div class="row">
<div class="col-md-9">

</div>
<div class="col-md-3">

</div>
</div>#}
{{ form_end(form) }}
