{% extends 'base.html.twig' %}

{% block title %}Implementation{% endblock %}

{% block body %}
    <h1>Implementation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ implementation.id }}</td>
            </tr>
            <tr>
                <th>Reference</th>
                <td>{{ implementation.reference }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('implementation_index') }}">back to list</a>

    <a href="{{ path('implementation_edit', {'id': implementation.id}) }}">edit</a>

    {{ include('implementation/_delete_form.html.twig') }}
{% endblock %}
